spring:
  data:
    rest:
      base-path: /rest
  application:
    name: araok
  datasource:
    url: jdbc:postgresql://localhost:5432/araok
    username: araok
    password: araok
    driver-class-name: org.postgresql.Driver
  flyway:
    baseline-on-migrate: true
    enabled: true
    url: jdbc:postgresql://localhost:5432/araok
    user: araok
    password: araok

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always
  health:
    defaults:
      enabled: true
      
 app:
   count-content-downloads: 1000